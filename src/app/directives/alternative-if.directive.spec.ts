import { AlternativeIfDirective } from './alternative-if.directive';

describe('AlternativeIfDirective', () => {
  it('should create an instance', () => {
    const directive = new AlternativeIfDirective();
    expect(directive).toBeTruthy();
  });
});
